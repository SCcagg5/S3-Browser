# Local build outputs and runtime state.
src/s3-browser
.s3-browser-data/
test/garage/volume/
*.zip

# Local credentials and developer overrides.
config.hcl
secrets/
*.pem
*.key
service-account*.json

# Repository metadata is not needed in image builds.
.git/
