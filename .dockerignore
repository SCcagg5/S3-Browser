# Local build outputs.
src/s3-browser
*.zip

# Local credentials and developer overrides.
config.hcl
secrets/
*.pem
*.key
service-account*.json

# Repository metadata and workstation files are not needed in image builds.
.git/
.DS_Store
